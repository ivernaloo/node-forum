{
  "name": "aloo-download",
  "version": "1.1.0",
  "description": "display the speed of downloading",
  "main": "index.js",
  "scripts": {
    "prepublish": "npm run compile",
    "test": "npm run compile && mocha --compilers js:babel-core/register",
    "compile": "babel src -d lib",
    "addUser": "npm adduser --scope=@aloo --registry=https://registry.npmjs.com",
    "npmPublish": "npm publish --registry=https://registry.npmjs.com"
  },
  "author": "aloocs@gmail.com",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ivernaloo/node-forum.git"
  },
  "devDependencies": {
    "babel-cli": "^6.23.0",
    "babel-core": "^6.23.1",
    "mocha": "^3.2.0"
  }
}
